# Supabase (required for auth)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key

# API server port (default 3001). Change if port is already in use.
# API_PORT=3002

# Carbonmark (certificate authentication)
# CARBONMARK_API_KEY=optional-override

# Storacha (optional – decentralized storage for certificates & listings)
# Create account & Space via CLI, then create key + delegation. See STORACHA.md.
# STORACHA_KEY=Mg...
# STORACHA_PROOF=base64-delegation-output
# STORACHA_LISTINGS_CID=optional-pinned-listings-cid

# ERC20 deploy (testnet). See CONTRACTS.md.
# DEPLOYER_PRIVATE_KEY=0x...   (wallet that pays gas; can be same as platform wallet)
# SEPOLIA_RPC_URL=https://rpc.sepolia.org  (or Alchemy/Infura URL)
